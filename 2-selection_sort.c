#include "sort.h"

/**
 * selection_sort -
 * @array:
 * @size:
 */
void selection_sort(int *array, size_t size)
{

	size_t i, j;
	int small, temp;

	for (i = 0; i < size - 1; i++)
	{
		if (array[i + 1] < array[i])
			small = array[i + 1];
		small = array[i]
	}
}
